# Cursor 实施与验收协议

> 目的：减少“一次性大改、只看测试变绿、实现偏离产品语义”的风险。  
> 本协议适用于 PR3 及之后所有批次；各 PR 的专用规格优先定义具体行为。

## 1. 无法承诺“100%”，但必须做到可证伪

Cursor 不得用以下表述作为完成证据：

- “代码看起来没问题”；
- “主要功能已完成”；
- “测试基本通过”；
- “按照文档实现了”；
- “应该不会影响其他模块”。

完成必须由可复现证据组成：

- 明确需求编号；
- 修改文件和符号；
- 自动化测试；
- 静态检查；
- 数据迁移结果；
- 人工验收；
- 未完成项；
- 回滚方法。

## 2. 每个 PR 的六个阶段

### 阶段 A：只读盘点

开始改代码前输出：

1. `git status --short`；
2. 涉及文件和已有用户改动；
3. 当前数据模型和接口；
4. 所有写入入口；
5. 当前测试、lint、build 基线；
6. 需要迁移的数据；
7. 规格差距矩阵。

此阶段禁止修改代码。

### 阶段 B：行为契约

把需求改写为：

- Given；
- When；
- Then；
- HTTP 状态码；
- 数据库变化；
- 不得发生的副作用；
- 日志/事件；
- 前端文案。

存在语义冲突时必须停下，不得自行选择最省事的解释。

### 阶段 C：测试先行

1. 新测试必须在旧实现上因正确原因失败；
2. 保存失败输出；
3. 不得降低原断言；
4. 不得把严格测试改成 skip/xfail；
5. 不得只 mock 掉真正要验证的数据库、权限或并发行为。

### 阶段 D：最小实现

- 只修改当前 PR；
- 业务规则只有一个权威实现；
- 路由和 UI 不复制后端规则；
- 不顺手重构无关模块；
- 不保留无期限兼容旁路；
- 不吞异常；
- 不新增真实密钥或个人数据日志。

### 阶段 E：分层验证

按顺序执行：

1. 领域单元测试；
2. 当前 Finding 回归；
3. 当前 PR 集成测试；
4. 完整后端测试；
5. 前端 scoped lint；
6. 前端全局 lint 基线；
7. 前端 production build；
8. 数据迁移 dry-run；
9. 人工冒烟；
10. 必要的 PostgreSQL/Redis/多 worker 集成测试。

某一层失败时必须区分：

- 本 PR 新增失败；
- 任务开始前已有失败；
- 环境问题；
- 规格尚未完成。

### 阶段 F：完成报告

逐项报告，不得只给总结。

## 3. 需求追踪矩阵

每个 PR 必须维护：

| Requirement ID | 用户结果 | 实现文件/符号 | 自动化测试 | 人工步骤 | 状态 |
|---|---|---|---|---|---|
| PRx-R01 | … | … | … | … | pass/fail/blocked |

规则：

- 每个用户结果至少一个测试或人工步骤；
- 每个安全/数据不变量必须有自动化测试；
- `pass` 必须附命令结果；
- `blocked` 必须描述阻断条件；
- 没有矩阵不得合并。

## 4. 变更边界

每批次在开始前写：

- `in_scope`；
- `out_of_scope`；
- 允许新增的表/字段/配置；
- 允许修改的接口；
- 禁止改变的兼容行为；
- 最大迁移影响；
- 回滚入口。

发现范围外问题：

1. 记录文件、行号、风险；
2. 加入对应未来 PR；
3. 当前 PR 不直接修，除非它阻断当前正确性；
4. 若必须扩展范围，先由用户确认。

## 5. 防止测试“假绿”

禁止：

- 删除测试；
- 弱化状态码或内容断言；
- 用 `try/except: pass`；
- 只检查 200 不检查数据；
- 测试通过但保留 XFAIL；
- 用 SQLite 结果代替 PostgreSQL 锁/约束验证；
- mock 掉授权、事务或持久化；
- 通过关闭 ESLint/安全规则解决错误；
- 只跑定向测试，不跑完整回归；
- 在测试 fixture 中直接制造最终状态，绕过被测业务动作。

必须检查：

- 正向路径；
- 越权路径；
- 资源不存在；
- 非法状态；
- 重复请求；
- 并发请求；
- 中途失败；
- 数据回滚；
- 旧数据兼容；
- 前端错误态。

## 6. 数据库变更规范

任何数据库变化必须包含：

- schema 设计理由；
- migration upgrade；
- migration downgrade；
- 旧数据 backfill；
- 空值和默认值；
- 索引/唯一约束；
- 大表锁风险；
- dry-run；
- 迁移前后行数与异常数；
- 备份/恢复说明。

启动时动态 DDL 不能替代正式迁移。过渡期新增动态 DDL 必须登记删除批次。

## 7. AI 功能额外门禁

AI 输出写入简历、Claim、审计或推荐前必须满足：

- 输出 schema 校验；
- prompt/model/version 记录；
- 输入来源与 allowed use；
- 原子事实来源覆盖；
- 无证据不得写回；
- 置信度不是模型自报；
- 降级路径；
- 超时和失败行为；
- 人工确认；
- 可删除和可更正；
- 不作“造假”“保证录用”等过度结论；
- 固定评测集和回归样本。

### 7.1 岗位适配与简历建议专用门禁

涉及“增强竞争力”、岗位画像、简历建议或 Potential action 的批次，还必须满足：

- 先输出结构化 `issue_type`，再生成策略或改写；
- 明确区分 `presentation_gap`、`evidence_gap`、`capability_gap` 和 `hard_constraint`；
- 对语义允许的问题提供至少两条实质不同的合法策略，不得用同义改写伪造多样性；
- `quantification` 不是默认策略，没有服务端数字证据时不得 `can_apply_now=true`；
- 没有数字时仍评估职责、方法、复杂度、相关性、作品证据和定性结果路径；
- `capability_gap` 只能生成真实学习/项目行动，完成前不得写成已有能力；
- `hard_constraint` 和 `credibility_risk` 不得通过润色 patch 消除；
- 每个策略回指 JD requirement、Claim/answer、来源和版本；
- 策略资格、最终 delta 和 confidence 由服务端规则/评分器决定，不信任客户端或 LLM 自报；
- 分开记录表达、证据和能力变化，不把简历措辞改善宣传为能力增长；
- 用户切换策略、拒绝建议和补充证据均可恢复、可追踪；
- 公司工作语境资料不能自动提升为录用要求，ESCO/O*NET 不能直接视为某公司的录用真值；
- 禁止以员工学校、年龄、性别、籍贯、现公司或其他敏感/代理属性生成“成功画像”。

专用固定回归集至少覆盖：

- 有真实数字且可量化；
- 没有数字但可以澄清职责；
- 没有数字但可以展示方法或权衡；
- 可以用定性结果或作品证明；
- 证据不足但不能断言能力不足；
- 真实能力缺口；
- 硬门槛；
- 来源冲突；
- 技术、产品、运营、设计、研究、管理和应届项目。

验收报告必须同时给出：issue 分类结果、候选策略、资格门禁、实际写回结果、三类 delta、来源追踪，以及量化建议占比和策略重复率。

## 8. 前端完成定义

不能以 production build 成功替代前端验收。至少检查：

- 目标文件 scoped lint；
- 全局 lint 基线；
- loading/empty/error/success；
- 401/403/409/422/429；
- 重复点击和请求中状态；
- 刷新后的持久状态；
- 候选人与招聘方不同文案；
- 岗位建议按“现在可优化 / 待补证据 / 真实提升 / 硬门槛”正确分栏；
- 同一 issue 可查看和切换合法策略，刷新后选择状态与服务端一致；
- “预览改写 / 回答问题 / 添加证据 / 创建成长计划 / 查看替代岗位”不会错误共用“采纳到简历”；
- 键盘可操作和基本可访问性；
- 移动端关键页面；
- API 返回旧数据时的兼容行为；
- 不根据错误自由文本实现安全规则。

## 9. 完成报告模板

```markdown
## 批次
PRx — 名称

## 范围
- in_scope:
- out_of_scope:

## 需求追踪矩阵
| ID | 结果 | 实现 | 测试 | 状态 |

## 修改
| 文件 | 符号/路由 | 原因 | 行为变化 |

## 数据库
- migration:
- backfill:
- downgrade:
- dry-run:

## 验证
| 命令 | 结果 | 说明 |

## 人工验收
1. …

## 兼容与回滚
- …

## 尚未完成
- …

## 工作区保护
- 确认未 reset/checkout/覆盖任务前改动：
```

## 10. 最终判定

只允许三种结论：

- **完成**：该 PR 专用 Definition of Done 全部满足；
- **部分完成**：核心实现存在，但仍有明确未过门禁；
- **阻断**：缺少用户决定、权限、环境或外部依赖，无法安全继续。

不得把“部分完成”写成“完成但还有一些优化项”。
