# QLink 下一轮可执行产品方案

> 版本：2026-08-03  
> 目的：把“帮助年轻人才看清方向、让企业看见潜力”的宏观愿景，收敛为一个能在 4 周内实现、验证和决策的产品闭环。  
> 本文承接现有产品战略，不替代长期愿景；本轮只验证一个最关键、最接近差异化的能力。

## 一、这轮到底要完成什么

### 1. 长期目标不变

QLink 的长期目标仍然是：

> 让非标准背景的人才更准确地看见适合自己的机会，并让企业基于真实证据而不是标签识别潜力。

这个目标太大，不能直接作为一轮开发任务。它同时包含职业探索、人才评估、申请材料、招聘协作和结果反馈等多个系统。

### 2. 本轮唯一核心目标

把宏观目标收敛为一个可测试的产品能力：

> 面对一项岗位要求，QLink 能根据用户现有证据选择正确的下一步：直接使用证据、立即追问、安排能力发展任务、标记现实约束，或者明确表示暂时无法判断。

这比“给候选人算一个匹配分”更有价值，也比“判断用户是真的不会还是没说清楚”更严谨。

原因是：仅凭一句模糊经历，系统和招聘者都不能可靠证明一个人“真的没有某项能力”。真正的能力缺口，必须经过岗位要求、完整背景、一次澄清和用户确认后才能成立。

### 3. 本轮待验证的产品主张

内部假设：

> QLink 能减少两种高成本误判：把“没说清楚”误判成“不会”，以及把“没有证据”误判成“已经具备”。

验证通过后，候选人侧可以使用的表达：

> 你不一定不会，可能只是没有说清楚。QLink 先帮你找到证据；真的还没具备时，再告诉你下一步怎么补。

验证前不能对外声称：

- “系统知道你真正具备什么”；
- “系统能准确判断你没有某项能力”；
- “比招聘者更懂候选人”；
- “能预测面试或录用概率”。

## 二、核心产品模型：不是“双缺口分类”，而是“下一步决策路由”

### 1. 五种状态

系统针对每一项岗位要求输出一个状态，而不是输出一个笼统总分。

| 状态 | 含义 | 系统动作 | 对用户的表达 |
|---|---|---|---|
| `supported` | 已有相关、可信且足够具体的证据 | 直接进入申请材料和面试故事 | “这项要求已有证据支持” |
| `clarify` | 有相关经历，但角色、深度、过程或结果不完整 | 当场追问一个最有信息量的问题 | “你可能做过，但还需要补充这一点” |
| `develop` | 澄清后确认目前没有足够的相关或可迁移证据 | 生成一个轻量发展任务，异步回填 | “目前证据不足，可以通过这个任务补齐” |
| `constraint` | 签证、地区、证书、学历等客观条件存在冲突 | 单独展示，不混入能力得分 | “这是现实约束，需要单独确认” |
| `unknown` | 信息不足、证据冲突或系统置信度不足 | 克制判断，补充信息或人工复核 | “目前无法可靠判断” |

### 2. 强制状态转移规则

状态必须遵循以下过程：

```text
岗位要求
  ↓
检索相关经历、Claim 和证据
  ↓
证据充分 ─────────────→ supported
  ↓ 否
存在相关经历但信息不完整 → clarify → 用户回答 → supported / develop / constraint / unknown
  ↓ 否
询问用户是否有未录入经历 → 有 → clarify
                         无 → develop
客观资格冲突 ───────────→ constraint
无法可靠判断 ───────────→ unknown
```

必须遵守三条底线：

1. 不得因为简历里没有关键词就直接判为 `develop`。
2. 不得把模型推测生成的内容当作用户证据。
3. 不得把 `unknown` 强行压缩成通过或不通过。

### 3. 真正的创新点

工作流分支、同步与异步分离、部分生成，都属于优秀产品的基本能力，不足以形成长期壁垒。

本轮真正值得验证的创新是：

> 同一套证据系统，不只判断“匹不匹配”，而是为每一项岗位要求决定最合适的下一步，并允许状态随着用户回答和新成果持续变化。

如果这个路由做得准，它可以同时服务：

- 候选人的方向判断；
- 简历和面试材料生成；
- 能力提升计划；
- 招聘者的证据化复核。

壁垒最终来自积累的标注案例、跨背景证据映射规则、状态变化数据和真实结果，而不是某个提示词。

## 三、用现有功能组成一条新的主流程

### 1. 两个入口

#### 入口 A：用户已有目标岗位

用户粘贴或选择一个 JD，直接进入“岗位要求—证据映射”，不经过方向探索。

#### 入口 B：用户没有明确目标

系统依据 Career Passport 和偏好，生成三个轻量方向：

- 当前可胜任；
- 相邻可迁移；
- 有挑战但值得发展。

这里只推荐岗位族或角色方向，不建设完整市场情报系统。用户选择一个方向后，进入同一条主流程。

### 2. 主流程

1. **一次性解析背景**  
   将教育、工作、项目、技能和已有 Claim 存入 Career Passport / Evidence Vault。

2. **提取目标要求**  
   从 JD 中提取 5–8 项真正影响决策的要求，区分能力要求与客观约束。

3. **检索并映射证据**  
   为每项要求寻找相关 Claim、项目、工作经历和已确认材料，保留来源。

4. **执行下一步路由**  
   将每项要求路由到 `supported`、`clarify`、`develop`、`constraint` 或 `unknown`。

5. **最多完成三次即时澄清**  
   优先询问最可能改变申请结果的问题。每次回答后立即更新状态和证据完整度。

6. **生成当前可用申请包**  
   无需等待全部缺口补齐。已支持的内容可生成简历要点、求职信素材和面试故事；未解决项清楚标注。

7. **把发展缺口变为异步任务**  
   每次只生成一个明确、可交付、可回填的任务，而不是一套课程推荐。

8. **轻量记录结果**  
   用户可随时记录投递、面试、拒绝或录用结果，为以后校准积累数据。

### 3. 用户第一次使用应看到什么

首屏不再是一个抽象匹配分，而是一个可行动结果：

- 目标岗位的 5–8 项关键要求；
- 已有证据支持几项；
- 哪几项只需要说清楚；
- 哪些属于发展任务或现实约束；
- 一个“生成当前可用版本”按钮；
- 一个优先级最高的澄清问题。

理想情况下，用户在五分钟内获得第一份可用产出。

## 四、对 Claude 方案的保留与修正

### 1. 保留的部分

- 有目标岗位的用户跳过方向探索；
- 机会信息按需读取，不建设长期市场情报库；
- 即时澄清和长期发展采用不同节奏；
- 申请材料允许部分生成；
- 招聘者复核与结果回填作为旁路触发；
- 先验证核心能力，再扩大功能范围。

### 2. 必须修正的部分

#### 不能只用模糊经历句子标注“能力缺口”

“参与了一个跨部门项目”只能说明证据不完整，不能说明候选人没有协作能力。标注单元必须包含目标要求、相关背景和澄清结果。

#### 不能只看准确率

如果测试集类别不均衡，70% 准确率可能毫无意义。必须同时看混淆矩阵、各类召回率、错误成本和系统放弃判断的能力。

#### 不能让招聘者单独定义真相

招聘者适合判断“证据对岗位是否充分”，候选人才知道“自己是否真正做过”。金标准需要双方信息，并在冲突时仲裁。

#### 30–50 条只适合预实验

30–50 条可以快速发现提示词和规则问题，但不足以支撑市场宣传。正式门禁至少需要 60–100 个“岗位要求 × 候选人背景”案例，并覆盖多个岗位族。

#### 现有招聘者功能不需要重做

当前已经存在 Screening、Claim Snapshot 和人工复核能力。本轮将其冻结为小范围试点功能，不新增完整招聘者系统，也不删除既有资产。

## 五、验证设计：先证明“路由有用”，再证明“分类准确”

### 1. 测试单元

每个案例不是一句孤立经历，而是一个完整的决策单元：

```json
{
  "target_requirement": "能独立推动跨部门项目",
  "candidate_context": "相关工作、项目、技能和教育背景",
  "original_claim": "参与跨部门项目",
  "retrieved_evidence": [],
  "clarification_question": "你在项目中负责了哪个决策或协调动作？",
  "candidate_answer": "...",
  "final_label": "supported | develop | constraint | unknown",
  "reviewer_reason": "..."
}
```

### 2. 样本组成

预实验：

- 30–50 个案例；
- 用于验证标签定义、流程和评测脚本；
- 不用于对外宣传。

正式试点：

- 12–20 名真实用户；
- 每人选择 3–5 项岗位要求；
- 合计 60–100 个案例；
- 优先覆盖产品、数据、AI / 软件三个团队最熟悉的岗位族；
- 同时包含标准背景和跨教育、跨行业、跨国家背景。

### 3. 金标准如何产生

1. 候选人确认自己是否做过、做到什么程度、能否提供具体细节。
2. 有招聘或领域经验的评审判断这些证据是否足以支持目标要求。
3. 双方意见不一致时，由第三位评审依据统一规则仲裁。
4. 记录初始状态与澄清后的最终状态，不能只记录最终标签。

建议先做 15 个双人重复标注案例，检查标签说明是否一致。若一致性过低，先修订标注规则，不调整模型迎合不稳定标签。

### 4. 对照实验

#### A 组：当前基线

- 关键词 / 固定权重匹配；
- 展示总分或笼统缺口；
- 统一生成通用追问。

#### B 组：新路由方案

- 以岗位要求为单位检索证据；
- 输出五种状态；
- 先澄清，再确认发展缺口；
- 允许 `unknown`；
- 生成当前可用申请材料。

比较的重点不是“B 有没有一个更高的分数”，而是 B 是否更常给出正确、可执行的下一步。

### 5. 核心指标和建议门禁

| 指标 | 为什么重要 | 本轮建议门禁 |
|---|---|---:|
| 路由 Macro-F1 | 防止多数类别掩盖少数类别错误 | ≥ 0.75 |
| 错误支持率 | 防止把没有证据说成已经具备 | ≤ 5% |
| 错误发展率 | 防止把没说清楚误判成能力不足 | ≤ 10% |
| 澄清解决率 | 提问后能否形成明确状态 | ≥ 60% |
| 问题可回答率 | 用户能否理解并回答追问 | ≥ 80% |
| 用户认为判断准确/有用 | 验证真实感知价值 | ≥ 80% |
| B 相对 A 的下一步有用率提升 | 证明新机制比基线有价值 | ≥ 15 个百分点 |
| 事实编造率 | 申请材料不能加入用户未确认事实 | 0% |
| 首次有用产出时间 | 验证流程效率 | 中位数 ≤ 5 分钟 |

这些是本轮决策门禁，不是永久行业标准。预实验后可根据类别分布调整，但不得在看到结果后为了“通过”而降低标准。

## 六、匹配算法 V4：最小必要改造

### 1. 算法角色重新定义

当前匹配算法不再负责输出一个看似精确的“候选人真相”。它拆为四层：

1. **约束层**：签证、地区、证书等，输出满足、冲突或未知；
2. **机会层**：在目标模式或探索模式下召回岗位；
3. **证据层**：把岗位要求映射到 Claim 和来源证据；
4. **路由层**：根据证据完整性和用户回答决定下一步。

最终界面优先展示状态和原因，不把所有信息压成一个总分。

### 2. 路由器的最小实现

第一版不训练新模型。采用“语义检索 + 结构化模型判断 + 确定性状态规则”：

- 语义检索负责找到可能相关的经历和 Claim；
- 模型负责判断关联性、缺失字段并生成一个澄清问题；
- 规则负责限制状态转移，防止无证据时直接判断能力不足；
- 用户回答和确认负责把 `clarify` 转为最终状态。

建议新增一个独立路由模块，例如：

```text
backend/app/opportunity_evidence_router.py
```

输入至少包括：

- 岗位要求及其类型；
- 相关 Claim、证据来源和确认状态；
- 用户偏好与客观约束；
- 历史澄清回答；
- 使用的模型、提示词、规则和数据版本。

输出必须是结构化结果，包含状态、依据、置信度、缺失字段和下一步动作。

### 3. 必须同步调整的旧逻辑

- 移除“名企经历”对行业匹配的特殊加分；
- 停止向用户展示未经真实数据校准的面试/录用概率；
- 将数字结果视为证据的一种，不把“没有数字”直接当作低影响力；
- Target 模式与 Explore 模式分开，探索模式不能被默认的历史职位意图过早过滤；
- LLM 重排不传姓名，加入完整项目、Claim、证据来源和确认状态；
- 招聘者侧继续保留通过、失败、未知和人工复核，不允许自动拒绝。

### 4. 现有模块如何复用

| 现有模块 | 本轮角色 |
|---|---|
| `career_vault.py` | 汇总背景、Claim、证据来源和确认状态 |
| `target_job_optimization.py` | 承载目标岗位要求拆解和问题列表 |
| `matching_hybrid.py` | 岗位与相关证据的候选召回，不再充当最终真相 |
| `matching_preference.py` | 明确区分 Target / Explore 两种模式 |
| `matching_signals.py` | 删除或隐藏名企加分、概率和不可靠成长潜力信号 |
| `matching_rerank.py` | 匿名、证据感知的结构化重排 |
| `personalized_guidance.py` | 将确认后的发展缺口变为一个异步任务 |
| `screening.py` | 在小范围招聘者试点中复用同一状态语言 |
| `claim_reasoning.py` | 提供 Claim 解释、来源和人工复核支持 |

## 七、四周实施安排

### 阶段 0：定义与预实验（第 1–3 天）

交付物：

- 五状态定义和状态转移规则；
- 标注手册 v1；
- 30–50 个预实验案例；
- 当前算法基线输出；
- 评测脚本和第一份混淆矩阵。

决策：

- 如果标注者无法稳定区分状态，先修正规则；
- 如果多数案例都缺少目标要求或候选人回答，补数据结构；
- 不在这个阶段设计更多页面。

### 阶段 1：后端路由闭环（第 1 周）

实施：

- 新增岗位要求—证据路由器；
- 记录每个状态的依据和来源；
- 建立强制状态转移；
- 建立 A/B 离线评测；
- 移除或隐藏高风险匹配信号和概率展示。

完成标准：同一案例可重复运行、可审计、可与基线比较，且不会生成未经确认的事实。

### 阶段 2：候选人主流程（第 2 周）

实施：

- 在 Advisor 中接入“有目标 / 无目标”入口；
- 展示岗位要求和五种状态；
- 每次最多提出三个高价值问题；
- 回答后即时更新状态；
- 支持生成“当前可用申请包”；
- 为 `develop` 生成一个异步任务。

完成标准：一个新用户可从背景录入走到部分申请包，不需要进入招聘者流程。

### 阶段 3：真实用户试点（第 3 周）

实施：

- 招募 10–15 名目标用户；
- 每人完成一个真实目标岗位流程；
- 将数据扩展到 60–100 个案例；
- 收集任务完成数据、主观反馈和观察记录；
- 选择少量用户自愿分享给招聘者或领域评审。

完成标准：每个核心指标都有数据，而不是只有访谈观点。

### 阶段 4：决策与收敛（第 4 周）

根据门禁作三选一决策：

1. **Go**：核心门禁通过，正式打磨产品表达并扩大试点；
2. **Iterate**：价值信号存在，但某类错误集中，限定岗位族继续优化；
3. **Stop / Pivot**：相对基线没有显著提升，停止把它作为主卖点，回到职业探索或证据生产中的其他高价值问题。

## 八、本轮范围边界

### 必须做

- 背景一次性导入和证据来源保留；
- 有目标 / 无目标入口分支；
- 按需解析一个目标 JD；
- 岗位要求—证据映射；
- 五状态路由；
- 最多三个即时澄清问题；
- 一个轻量发展任务；
- 部分申请包生成；
- 全流程事件记录；
- 基线对照和真实用户试点。

### 本轮明确不做

- 独立市场情报数据库；
- 课程或项目推荐引擎；
- 图数据库迁移；
- 新建完整招聘者产品；
- 结果分析大屏；
- 自动拒绝候选人；
- 支付体系扩建；
- 未经真实合作验证的高校、企业或招聘机构背书。

## 九、数据与埋点

本轮至少记录以下事件：

- `workflow_started`
- `entry_mode_selected`
- `target_selected`
- `requirement_extracted`
- `evidence_retrieved`
- `requirement_routed`
- `clarification_asked`
- `clarification_answered`
- `clarification_resolved`
- `evidence_confirmed`
- `development_task_created`
- `development_task_completed`
- `partial_pack_generated`
- `application_item_exported`
- `outcome_recorded`
- `user_routing_feedback_submitted`

每次路由还要保存：

- 模型版本；
- Prompt 版本；
- 规则版本；
- 岗位要求来源；
- 使用了哪些 Claim 和证据；
- 初始状态、最终状态及改变原因；
- 用户是否同意该判断。

任何用于分析的数据都应匿名化，并避免把用户姓名等无关信息传入重排和判断模型。

## 十、最终交付清单

四周结束时必须具备以下可审计成果：

1. 五状态产品与标注规范；
2. 60–100 个真实决策案例；
3. 当前基线与新路由方案的对照报告；
4. 一个可以从目标岗位走到部分申请包的真实流程；
5. 事件字典与数据看板或最小分析查询；
6. 10–15 名用户的试点报告；
7. 错误案例库及改进优先级；
8. 明确的 Go / Iterate / Stop 决策记录。

## 十一、Definition of Done

本轮不是以“页面做完”或“功能上线”为完成，而是同时满足：

- 用户能够完成端到端流程；
- 事实编造率为零；
- 错误支持率和错误发展率低于门禁；
- 新方案相对当前基线的下一步有用率显著提高；
- 用户在五分钟内看到可用产出；
- 所有判断可追溯到岗位要求、Claim 和证据来源；
- 团队能根据真实数据作出继续、限定范围或停止的决策。

如果只完成界面和接口，没有得到上述验证数据，本轮仍然不算完成。

## 十二、下一步：今天就能开始的工作

按以下顺序执行，不再继续扩展宏观方案：

1. 锁定三个试点岗位族：产品、数据、AI / 软件；
2. 写出五状态标注手册 v1，并各准备正例、反例和边界例；
3. 从真实 JD 中提取 30–50 个“岗位要求 × 候选人背景”预实验案例；
4. 用现有匹配逻辑跑出 A 组结果；
5. 用新规则人工模拟 B 组路由，确认状态模型确实能产生不同且更好的下一步；
6. 只有预实验成立后，再开始实现路由器和 UI 编排。

这一方案把长期愿景、市场卖点和本轮工程目标分开：长期仍然建设人才机会与证据网络；本轮只证明 QLink 是否能避免把“没有写出来”当成“没有能力”，并把每个缺口转化成正确、可执行的下一步。
