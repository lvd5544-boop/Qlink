# PR8 / PR9 人工网页验收指南（逐步）

> 目标：只用浏览器完成验收。平台入口固定为 **一个网页**。  
> 证据请记入同目录 `PR8_PR9_MANUAL_ACCEPTANCE_EVIDENCE.md`。

## 0. 准备环境（只做一次）

### 0.1 打开系统

1. 确认 Docker Desktop 已启动（托盘图标正常）。
2. 若服务尚未起来：在项目目录执行一次 `make up`（之后验收全程不用再开终端）。
3. 浏览器打开：

```text
http://localhost:8080
```

4. 另开两个标签页确认：

```text
http://localhost:8080/api/health
http://localhost:8080/api/ready
```

预期：`health` 为 ok；`ready` 为 ready；未配模型时可为 `rules_only`。

### 0.2 准备两个浏览器窗口

| 窗口 | 用途 |
|---|---|
| 普通窗口 | 招聘方 B |
| 无痕窗口 | 候选人 A |

避免同一浏览器互相顶掉登录态。

### 0.3 创建候选人 A

1. 无痕窗口打开：`http://localhost:8080/register`
2. 邮箱示例：`candidate.a.manual@example.com`
3. 密码示例：`CandidateA9x!`（≥10 位，含大小写+数字）
4. 注册 → 登录 → 应进入求职者中心

截图：注册成功 / 登录后首页。记入证据表 **E0-1**。

### 0.4 创建招聘方 B

公开注册页**不能**注册招聘方。用邀请码开通（把邀请码换成你 `.env` 里的 `EMPLOYER_INVITE_CODE`）：

```bash
curl -X POST http://localhost:8080/api/auth/register-employer \
  -H 'Content-Type: application/json' \
  -d '{"email":"employer.b.manual@example.com","password":"EmployerB9x!","invite_code":"你的EMPLOYER_INVITE_CODE"}'
```

然后在普通窗口打开 `http://localhost:8080/login`，用招聘方账号登录，应进入招聘管理。

截图：招聘方登录后首页。记入 **E0-2**。

### 0.5 招聘方 B 发布测试岗位

1. 左侧点「发布岗位」→ URL 类似 `/employer/post-job`
2. 粘贴：

```text
岗位：后端工程师
地点：上海
要求：Python、FastAPI、PostgreSQL，3 年以上相关经验
职责：负责招聘平台 API、数据库与匹配服务开发与维护
```

3. 点「提交文本」→ 到「我的岗位」确认岗位存在

截图：岗位详情 / 我的岗位列表。记入 **E0-3**。

### 0.6 准备候选人 A 的测试简历（TXT 即可）

保存为桌面文件，例如 `candidate-a-manual.txt`：

```text
姓名：验收候选人A
邮箱：candidate.a.manual@example.com
期望职位：后端工程师
技能：Python, FastAPI, PostgreSQL, Redis

工作经历 1：某科技 后端工程师 2022-2025
负责招聘平台 API 与匹配服务，日请求峰值约 10 万+，并完成数据清洗与报表复核。

工作经历 2：某创业公司 开发 2021-2022
参与业务系统开发（具体指标与本人职责待补充）
```

说明：

- 经历 1：有可验证数字成果（10 万+）
- 经历 2：故意不完整 → 后面用作「信息不足」主张

### 0.7 候选人上传简历并进入工作台

1. 无痕窗口候选人登录 → 左侧「上传简历」→ 选上面 TXT
2. 左侧「我的简历」→ 找到该简历 → 点 **「进入工作台」**
3. 页面标题应为「我的简历 · AI 工作台」
4. 向下滚动，应能看到「④ 履历主张（Claim Passport）」

若主张为空：点面板底部「同步当前简历主张」。

截图：工作台总览 + Passport 标题区。记入 **E0-4**。

---

## 1. PR8：候选人 Claim Passport

**URL 预期**：`http://localhost:8080/candidate/my-resumes`（已进入工作台）

### 1.1 文案检查

在「④ 履历主张（Claim Passport）」顶部 Alert：

- 必须类似：**「记录来源与处理历史，不代表事实已认证」**
- 不得出现：`认证通过`、`已验证真实`、`事实认证通过`

截图整块 Alert。记入 **E1-1**。结果：通过 / 不通过。

### 1.2 展开一条主张

1. 点击某条主张折叠标题展开。
2. 核对是否可见：
   - 原文
   - 路径 / 类型
   - 证据状态标签（如「信息不足」「有用户证据支持」）
   - 处理状态标签（如「待补充」「用户已说明」）
   - 「修改与处理历史」

截图展开态。记入 **E1-2**。

### 1.3 补充说明

1. 筛选「证据状态」选「信息不足」，或直接选经历 2 对应主张。
2. 证据类型选「用户说明」或「指标背景」。
3. 填写：

```text
该指标统计周期为 2025 年 Q1，负责数据清洗与报表复核
```

4. 点 **「保存补充说明」**
5. 预期右上角/消息提示类似：**已保存为用户证据，不等同于第三方认证**
6. 该主张标签应变为「有用户证据支持」或合理状态；处理状态可为「用户已说明」

截图：保存前后对比。记入 **E1-3**。

### 1.4 撤回

1. 在刚新增证据旁点 **「撤回」**
2. 预期显示 **「已撤回（内容已脱敏）」**
3. 不应再显示完整说明原文

截图。记入 **E1-4**。

**本节验收要点**：补充/撤回可用；始终称「用户证据」，不是事实认证。

---

## 2. PR8：投递快照与招聘方隔离

### 2.1 候选人投递

1. 候选人左侧「浏览岗位」→ 找到招聘方 B 的测试岗位
2. 「申请岗位」→ 选刚上传的简历 → 提交
3. 「已申请岗位」确认有一条申请

截图。记入 **E2-1**。

### 2.2 投递后再补证据（关键）

1. 回到「我的简历 → 进入工作台 → Claim Passport」
2. 再新增一条**不同**说明，例如：

```text
投递后补充：监控窗口为工作日晚高峰一小时（仅候选人私密补充）
```

3. 保存成功

截图。记入 **E2-2**。（这条应只在候选人侧可见）

### 2.3 招聘方查看快照

1. 普通窗口招聘方登录 → 「申请管理」（或从「我的岗位」进申请列表）
2. 打开候选人 A 对该岗位的申请详情
3. 找到 **「履历主张快照」**
4. 检查：
   - 标题区提示：**「仅显示本次投递快照」**
   - 描述含：**「有用户证据支持」不等于事实认证**；招聘方看不到之后新增私密补充
   - 列表内容应是**投递当时**快照，**不应**出现「投递后补充：监控窗口…」那段文字

截图整块「履历主张快照」。记入 **E2-3**。

**本节验收要点**：快照不可被后续修改污染；招聘方不能把自述当平台认证。

---

## 3. PR9：可提升空间模拟

### 3.1 打开模拟面板

1. 切回候选人无痕窗口 → 我的简历 → 进入工作台
2. 若「可提升空间模拟」未出现：
   - 确认上方「匹配岗位」至少有一条（可先浏览岗位/等待匹配刷新）
   - 面板依赖 `topMatches[0]`，没有匹配则不会渲染
3. 找到标题 **「可提升空间模拟」**
4. 固定免责声明必须是：

```text
模型内模拟，不代表面试或录用承诺。
```

5. 记录页面上的：
   - 当前 X.X 分
   - 模拟后 Y.Y 分

截图。记入 **E3-1**。

### 3.2 四栏检查

建议应归入四类之一（允许某栏为空）：

- 现在可以优化  
- 需要补充证据  
- 需要真实提升  
- 硬门槛  

截图四栏。记入 **E3-2**。

### 3.3 勾选并重新模拟

1. 勾选一个「现在可以优化」或「需要补充证据」策略（「纳入模拟」）
2. 点 **「按所选策略重新模拟」**
3. 记录新的当前分 / 模拟后分 / 建议是否变化
4. 取消勾选后再点「按所选策略重新模拟」，观察是否回到相应结果

> 说明：若分数几乎不变，记「观察结果」并截图；独立审计曾发现部分策略只改 planned skills，分数变化可能很小，这属于产品边界，应如实记「通过观察 / 不通过」。

截图。记入 **E3-3**。

### 3.4 策略操作按钮

对可见按钮逐一点击，预期（当前实现）：

| 按钮文案 | 预期行为 |
|---|---|
| 预览改写 | 页面滚动到 Resume Coach（工作台上方 `#resume-coach`） |
| 回答问题 / 补充证据 | 滚动到 Claim Passport（`#claim-passport`） |
| 创建成长计划 | 面板内出现「成长计划：…」成功提示条 |
| 查看替代岗位 | 跳转「浏览岗位」 |

若按钮缺失或无反应：记 **不通过**，附截图与 URL。

截图。记入 **E3-4**。

### 3.5 状态记录按钮（不改简历）

依次点：

- 暂不采用  
- 标记已采纳  
- 标记已完成  

预期每次提示类似：**已记录你的策略状态；简历不会被自动修改。**  
再打开简历原文/主张，确认**没有被自动改写**。

截图消息 + 简历未变。记入 **E3-5**。

**本节验收要点**：模拟是建议与记录工具，不是自动改简历，更不是录用预测。

---

## 4. PR9：硬门槛与安全边界

### 4.1 硬门槛组合

1. 招聘方再发一个硬条件岗位，例如：

```text
岗位：高级 Java 工程师
要求：5 年以上 Java 开发经验，精通 Spring
职责：核心交易系统研发
```

2. 候选人仍用「Python 后端、无 Java」的简历进入工作台，等待匹配该岗位后看模拟面板  
   （若面板只绑第一条匹配：可先确保该硬门槛岗位成为 top match，或新传一份明显不匹配简历专门测）
3. 预期：相关建议进入 **「硬门槛」** 或 **「需要真实提升」**
4. 不应出现「直接在简历编写/补造 5 年 Java 经历即可满足」这类可一键造假建议

截图。记入 **E4-1**。

### 4.2 无来源数字

对「信息不足」且无用户证据数字的主张：

- 模拟侧不应把「直接添加具体 KPI 数字」标成可立即写回  
- Claim Passport 补充说明仍是用户自述，不是认证

截图。记入 **E4-2**。

### 4.3 敏感属性

整段操作中，建议文案不应出现以年龄、性别、籍贯、学校层级等作为改进依据。

截图任意相关建议区。记入 **E4-3**。

---

## 5. 记录结论

在 `PR8_PR9_MANUAL_ACCEPTANCE_EVIDENCE.md` 中：

1. 每项标 **通过 / 不通过**
2. 不通过必须附：截图文件名、角色、URL、复现步骤
3. 汇总：

| 批次 | 人工网页验收 | 说明 |
|---|---|---|
| PR8 | 1–2 节全通过才可标「完成」 | 还需满足你们流程中的远端 CI/推送要求时一并核对 |
| PR9 | 3–4 节产品路径通过可写「代码与产品验收完成，真实试点校准持续进行」 | ESCO/O*NET 正式映射、试点样本、公平性人工评审 **不算** 已完成 |

---

## 常见卡住点

| 现象 | 处理 |
|---|---|
| `/api/ready` 503 | Docker 未起完；等 1–2 分钟或看 Docker Desktop 容器是否 healthy |
| 招聘方注册失败 | 邀请码与 `.env` 不一致 |
| Passport 为空 | 点「同步当前简历主张」；确认简历已解析出工作经历 |
| 无「可提升空间模拟」 | 尚无匹配岗位；先浏览/投递相关岗位，或等匹配刷新后再进工作台 |
| 招聘方看到投递后补充 | **不通过**：快照隔离失败 |
| 出现「认证通过」文案 | **不通过**：文案门禁失败 |
