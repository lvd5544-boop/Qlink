# PR9 试点校准与人工评审协议

本协议是 PR9 的验收资产，不是已完成的试点结果。任何报表在满足下列最小样本、人工评审和法律基础前，都只能称为“观察数据”，不得把模型内 delta 写成面试或录用概率。

## 数据边界

- 使用 `/admin/potential-simulation/pilot-metrics` 的聚合事件；接口不返回候选人、简历、岗位或敏感属性。
- 若需按合法分组分析，沿用 PR7 的 `legal_basis_attested=true` 机制并只传去标识化分组；不采集年龄、性别、籍贯、学校层级等敏感或代理属性。
- 评审样本须取得用途同意；原始材料不得进入普通产品分析报表。

## 每周检查

1. 记录样本量、查看→选择、选择→采纳、选择→完成率；记录量化策略占比与策略重复率。
2. 对每个 delta 桶抽取候选人自评“可执行/不清晰/不现实”，并由招聘从业者独立抽检动作是否现实、清晰、无冒犯。
3. 人工标注 issue 类型，重点计算 `evidence_gap` 被错判为 `capability_gap` 的比例；核对硬门槛和冲突从不生成简历 patch。
4. 在有足够、合法且去标识化的后续行为数据前，只比较观察分布；不得发布“delta 提升录用率”的结论。

## 发布门槛

- 固定跨岗回归集持续通过，且无来源量化建议为零。
- 每类岗位均有候选人与招聘从业者抽检记录，问题已闭环或明确标注风险。
- 公平性分析只在样本量、法律基础和分组质量足够时运行；不足时明确显示“样本不足”。
- 任何面向用户的文案保留“模型内模拟，不代表录用承诺”。
