# QLink R3-Converged：求职决策、面试训练与结果学习方案

> 日期：2026-08-05<br>
> 决策：以 R3-Lite 为首发产品，以 Third Round 的状态审计与规模化设计作为按门槛启用的第二层；不一次建设完整双端平台。<br>
> 首发边界：中国大陆；互联网、软件、数据、AI、产品等相邻数字岗位族；应届到工作三年内。

## 1. 最终产品承诺

QLink 首发只完成一个反复运行的个人求职决策循环：

```text
真实背景
  → 同领域目标或相邻方向
  → 岗位要求与证据映射
  → 说清楚 / 现在可用 / 发展行动 / 现实约束 / 待验证假设
  → 短而可用的申请与面试训练卡
  → 真实投递和面试结果
  → 下一轮复盘
```

用户可理解的承诺：

> 告诉 QLink 你真正做过什么，以及你想申请的岗位。系统帮助你看清现在怎么投、面试时怎样讲清真实经历、下一步最值得补什么，并根据真实求职结果调整下一次行动。

产品不承诺录用概率，不把简历缺词解释成能力不足，也不把一次拒绝解释成某项能力缺失。

## 2. 对五个问题的评判

### 2.1 没有目标岗位时，匹配机制必须优化

判断：**合理且必须做，但不是让匹配总分更复杂。**

当前代码已有 `focused / balanced / explore` 和职业族门禁，能过滤许多明显跨领域岗位；问题出在探索展示层把没有经过匹配评估的普通岗位默认归入“挑战方向”。这会把“不同领域”误包装成“值得挑战”。

新机制采用两阶段选择：

1. **职业族门禁**：先确定候选人的一个主职业族和最多两个相邻职业族；未知时先让用户确认，不做全市场随机推荐。
2. **族内排序**：只在允许职业族中按可迁移技能、任务相似度、用户偏好、现实约束和证据覆盖排序。

三条方向的定义：

| 方向 | 必须满足 | 不允许 |
|---|---|---|
| 当前 | 同职业族；已有任务或技能证据 | 只因为岗位热门或公司知名 |
| 相邻 | 相邻职业族；至少两项可迁移技能或一类相似任务 | 仅靠文本关键词重叠 |
| 挑战 | 同一或相邻职业族；差距可由一个 1–2 周作品或任务验证 | 完全不同职业族、需要长期资质转换 |

O*NET 将任务、职业特定信息和可迁移技能分层，ESCO 也把职业与技能概念通过层级和关系组织起来；这支持“先职业族、再技能任务”的设计，而不是跨全市场使用一个总分。[O*NET Content Model](https://www.onetcenter.org/content.html)、[ESCO Classification](https://esco.ec.europa.eu/en/classification)

MVP 不建设完整职业图数据库。先维护一个版本化的 `role_family_crosswalk` 配置，并记录每个方向的 `role_relation`、可迁移技能依据和被排除原因。

### 2.2 申请素材应进入 AI 面试训练和结果闭环

判断：**合理，而且这是比“下载一个文档”更强的产品闭环。**

“申请素材包”改名为“本次机会准备卡”，页面默认显示五个短栏目：

| 栏目 | 最大长度 | 用途 |
|---|---:|---|
| 现在可用 | 3 条，每条 90 字 | 简历和申请表达 |
| 面试故事 | 2 个，每个 4 个折叠字段 | AI 模拟面试的训练素材 |
| 待说清 | 3 个问题 | 补充角色、方法、范围或结果 |
| 一个行动 | 1 项 | 1–2 周能力或作品任务 |
| 未解决要求 | 3 项 | 防止用户误以为申请已经完备 |

TXT/PDF/DOCX 只作为次要导出，不作为主产品界面。

AI 面试官的输入使用同一份机会准备卡：

- 从岗位要求选择问题；
- 从用户确认的 Claim 选择故事锚点；
- 针对待说清项追问；
- 训练后只写入 `InterviewObservation`，经用户确认才更新 Claim；
- 不把模型生成的优秀答案直接写成用户经历。

结果获取边界：

- 平台内申请：系统可直接记录状态事件；
- 用户导入的站外 JD：只能由用户记录，或未来通过用户授权的邮件/ATS 集成获取；
- 不得暗示平台能自动知道所有企业的拒绝原因。

结果进入下一轮分析时采用来源分级：`platform_observed`、`employer_confirmed`、`candidate_reported`。解释规则：

- 进入面试：说明本次表达和基本相关性可能有效，不证明所有能力已具备；
- 拒绝：只形成“需要复盘”的信号；没有具体反馈时不得自动生成能力缺口；
- 录用：是本次流程的强结果，但不能外推为所有岗位都适合；
- 无结果：不得当作拒绝。

招聘结果具有“选择性标签”问题：只有经过某些决策的样本才有后续结果，而且人类决策本身可能受不可观测因素影响。因此结果适合做个人复盘和产品校准，不能直接作为无偏训练真值。[Human Decisions and Machine Predictions](https://www.nber.org/papers/w23180)

### 2.3 “暂时无法判断”可以推理，但推理不能冒充事实

判断：**思路部分合理，产品实现必须增加严格边界。**

将 `unknown` 改成“待验证假设”，系统可以依据已有信息生成：

- 最可能的两种解释；
- 每种解释引用的真实来源；
- 可能推翻它的一个问题或任务；
- 置信等级：低 / 中，不显示伪精确百分比；
- 验证状态：`hypothesis / user_confirmed / evidence_supported / rejected`。

允许推理做的事：排序追问、生成对照情境、设计训练题、生成合成评测案例、提出可验证假设。

不允许推理做的事：新增用户经历、把推断写入简历、把推断作为招聘筛选事实、把模型自产标签循环训练后声称准确。

“类似具身智能通过交互生成经验”的类比可以用于产品的交互学习，但求职场景的环境反馈稀疏、带偏差且不可完全观测。安全的数据闭环应为：

```text
真实来源 → 模型提出假设 → 用户/行为/结果验证
       ↘ 合成案例只进入隔离评测集或候选训练池
```

研究表明，不加区分地递归使用模型生成数据可能造成模型坍塌并丢失原始分布的尾部信息。因此合成数据必须保留来源标识、与真实数据分层、通过规则或第二模型验证，并始终保留真实数据锚点和独立真实验证集。[Nature: AI models collapse when trained on recursively generated data](https://www.nature.com/articles/s41586-024-07566-y)

MVP 阶段不微调自有模型。先用版本化 Prompt、检索、规则和离线评测验证“假设是否帮助用户更快说清楚”；只有积累足够真实确认样本后才评估训练。

### 2.4 用户增多后的维护风险

判断：**风险真实，但不能用提前拆微服务来规避。**

首发继续采用模块化单体；按容量和故障信号逐级增加基础设施：

| 阶段 | 触发条件 | 必须补齐 |
|---|---|---|
| 试点 | ≤100 活跃用户 | 幂等写入、所有权测试、模型超时与降级、人工运营表 |
| 小规模 | >100 DAU 或 P95 诊断 >5 秒 | AI/解析异步队列、任务重试和去重、限流、缓存、结构化日志和追踪 |
| 成长 | >1,000 DAU 或单表 >1,000 万事件 | `OpportunityWorkflow / RequirementRouteEvent`、归档策略、读写索引、租户级审计 |
| 扩张 | 单实例持续 CPU >60% 或明确隔离需求 | 多 worker/多实例、队列水平扩展、只读副本；按证据决定是否拆服务 |

FastAPI 官方支持多 worker 利用多核，但同时指出复制进程只解决部署问题的一部分；异步任务必须保持幂等，因为任务可能被重投。[FastAPI Server Workers](https://fastapi.tiangolo.com/deployment/server-workers/)、[Celery Tasks](https://docs.celeryq.dev/en/v5.5.0/userguide/tasks.html)

租户隔离采用两层：应用层所有权检查必须覆盖每个资源入口；生产 PostgreSQL 在高风险用户数据表上逐步启用 Row-Level Security，形成数据库层默认拒绝的第二道防线。[PostgreSQL Row Security](https://www.postgresql.org/docs/17/ddl-rowsecurity.html)

只有查询和索引数据证明需要时才做分区，不能把分区当作默认架构。[PostgreSQL Partitioning](https://www.postgresql.org/docs/current/ddl-partitioning.html)

### 2.5 历史残留、代码清晰度和分支规范

判断：**合理，应持续执行，但必须区分“可证明的残留”和“仍被生产路径使用的旧接口”。**

本轮立即清理：

- 私有目标岗位的跨候选人访问缺口；
- 名企经历对行业不匹配的加分；
- 学校名望展示信号；
- 启发式面试/录用概率；
- LLM 重排输入中的候选人姓名；
- 被 `.gitignore` 忽略的编辑器备份和旧 ZIP。

暂不删除：仍被前端或测试调用的旧评分字段、兼容接口和历史迁移。先用调用图、弃用日志和至少一个发布周期证明无流量，再删除。

分支和提交规范以 [CONTRIBUTING.md](./CONTRIBUTING.md) 为准。采用 `<type>/<area>-<purpose>`，例如 `feat/matching-domain-lanes`、`sec/target-job-ownership`。GitHub 建议使用短而描述性的分支名；已有共享分支不能在脏工作区或未核对 CI/PR 引用时直接重命名，因为重命名后本地拉取和部分自动化引用不会自动迁移。[GitHub Flow](https://docs.github.com/en/get-started/using-github/github-flow)、[Renaming a branch](https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/managing-branches-in-your-repository/renaming-a-branch)

## 3. 合并后的产品流程

```text
进入 QLink
  → 导入真实背景
  → 有目标：粘贴真实 JD
     无目标：只看同职业族的当前 / 相邻 / 挑战方向
  → 确认 5–8 项岗位要求
  → 证据映射
  → 已支持 / 需要说清 / 现实约束 / 待验证假设
  → 用户确认暂无相关经历后，才能进入发展行动
  → 生成短版机会准备卡
  → 使用同一批真实素材进行 AI 模拟面试
  → 投递并记录来源分级的结果
  → 复盘哪些表达被采用、哪些问题反复出现、下一步验证什么
```

## 4. 实施阶段

### Phase C0：可信度与维护基线（立即，1–2 天）

- 修复私有目标岗位跨用户访问；
- 移除名企/名校信号和伪概率；
- LLM 输入最小化；
- 清理编辑器备份；
- 建立分支和提交规范；
- 保留完整回归测试。

门禁：跨用户测试为 404；匹配输出无学校名望和面试/录用概率；测试、构建通过。

### Phase C1：有目标岗位主链路收口（3–5 天）

- 私有 JD 历史可由 owner 重新选择；
- `capability` 默认进入“需要确认”，不能直接显示“暂未具备”；
- 用户明确确认暂无相关经历后才生成发展行动；
- 一页回答“岗位要什么、我已有何证据、今天做什么”。

门禁：缺少关键词永远不能直接产生发展结论；用户第二天能恢复自己的私有 JD。

### Phase C2：同领域三方向探索（3–5 天，非阻塞）

- 建立版本化职业族与相邻关系配置；
- 未经过 `preference_policy` 的岗位不得出现在方向卡；
- 每个方向必须展示可迁移技能、相似任务和现实差距；
- 没有安全候选岗位时显示空状态，不能用跨领域岗位填满栏目。

门禁：数据/AI 用户不能收到餐饮、销售、医疗等不同职业族作为“挑战方向”。

### Phase C3：机会准备卡与面试训练（5–7 天）

- 把历史申请素材生成器重构为 `OpportunityPreparationCard` 短卡片投影；
- 同一素材进入结构化 AI 面试；
- 训练观察经用户确认后才写回 Claim；
- 导出降级为次要操作。

门禁：首屏栏目不超过五个，每个栏目符合长度上限；用户能从一条素材直接开始一次针对目标岗位的模拟面试。

### Phase C4：结果回写和下一轮复盘（5–7 天）

- 统一平台内和站外结果时间线；
- 保存结果来源、时间和可选原始反馈；
- 下一轮只用结果调整追问与优先级，不自动修改能力事实；
- 展示“因为哪条结果，本次建议发生了什么变化”。

门禁：拒绝且没有原因时，系统不能新增能力缺口；所有建议变化可追溯。

### Phase C5：推断假设与隔离评测（5–7 天）

- `unknown` 输出最多两个待验证假设；
- 保存来源、规则/Prompt/模型版本和验证状态；
- 合成数据只进入带 `synthetic` 标签的隔离评测池；
- 真实确认样本单独作为验证集。

门禁：任何假设都不能进入简历、Claim 或招聘结论，除非经过确认或证据支持。

### Phase C6：真实用户试点与扩展决策（至少 2 周）

- 20 名目标用户，每人使用真实 JD 和至少两段经历；
- 与通用大模型进行交叉对照；
- 记录首次有用产出、模拟面试完成、申请采用、7 日第二行为和付款；
- 继续沿用 R3-Lite 的 Go / Iterate / Stop 门禁。

只有出现下列信号，才从 Third Round 启用完整状态事件模型：

- 用户需要跨多个 JD 查看同一要求的历史变化；
- 人工无法稳定审计路由变化；
- 需要自动计算错误支持率或错误发展率；
- 事件量或团队协作已经超出 `OptimizationIssue` 的表达能力。

## 5. Third Round 的保留与延后

立即保留：五状态语义、来源链接、最多三问、事实确认、AI 可关闭、结果可追溯、评测和商业门禁。

延后建设：`OpportunityWorkflow`、`RequirementRoute`、路由事件表和持久化申请包。它们是规模化审计基础设施，不是验证 20 人价值所必需的首发功能。

禁止提前建设：完整市场数据库、全功能 ATS、自动拒绝、录用概率、基于学校或公司品牌的加分、没有真实验证集的自训练流水线。

## 6. 最终完成定义

本方案完成必须同时满足：

- 无目标用户只看到同一或相邻职业族的三个方向；
- 简历没写的内容不会被直接判为不具备；
- 机会准备卡短、可视化，并直接服务模拟面试；
- 平台内结果自动记录，站外结果明确由用户或授权集成提供；
- 下一轮分析能解释如何参考结果，但不把拒绝当作能力真值；
- 推断始终是带来源和验证状态的假设；
- 私有数据隔离、模型降级、幂等和测试门禁通过；
- 真实用户试点给出 Go / Iterate / Stop 结论；
- 只有达到明确规模门槛后才启用 Third Round 的完整事件模型。
