# QLink R3 执行与验收报告

> 日期：2026-08-09<br>
> 范围：R3 C0–C3，本轮重点为 C3“机会准备卡与面试训练”。

## 1. 结论

R3 已从 C0–C2 推进到 C3。当前代码、前端构建、规则与类型门禁、SQLite 全量回归、PostgreSQL 并发门禁，以及两条真实浏览器主链路均已通过。

本报告的“通过”表示所列风险和场景已被自动化验证，不表示软件在所有未知环境中绝对不可能存在缺陷。

## 2. 本轮完成内容

### GitHub 对外交付

- 保留可运行 Demo 与生产式 Docker Compose 交付；
- 根目录 README 重写为中英双语项目首页；
- 前端默认 Vite README 替换为 QLink 项目说明；
- 将 Cursor、历史 PR 报告、本地工作目录和演示文稿工作区纳入上传过滤；
- 39 份已跟踪历史过程文档已从 Git 索引移除，本地文件保留；
- 对外保留 README、R3 总计划、本执行报告、贡献规范、公平性基线和人工验收指南。

### 结构与命名

- `CandidateApplicationKit` → `OpportunityPreparationCard`；
- `CareerDirectionExplorer` → `CareerPathExplorer`；
- `candidateRouteState` → `requirementReadinessState`；
- 机会准备的纯数据投影与界面组件分离，可单独测试；
- 153 个 Python 文件已经过统一 Ruff 格式化检查。

### Phase C3：机会准备卡与面试

- 申请素材改为五个短栏目：现在可用、面试故事、待说清、一个行动、未解决要求；
- 每条短内容最长 90 字，数量上限按 R3 规则固定；
- 证据冲突和未确认 Claim 不会进入“现在可用”；
- 未确认经历只进入待说清问题；
- TXT 导出降级为次要操作；
- 机会准备卡可将同一份简历和目标岗位直接带入结构化面试；
- 面试观察结果仍需用户确认后才能写回；
- 新增面试入口对他人私有 JD 的 404 所有权门禁。

### 验收中发现并修复

- PostgreSQL 全量回归首次执行发现，正式 PR12 迁移已包含 `interview_results.structured_session_id`，但测试库的增量初始化只补到 PR5.1；
- 测试库初始化现已补入正式 `20260729_pr12_interview_sessions_up.sql`，历史 PR5.1 迁移往返测试结束前也会恢复当前 PR12 schema；
- 历史迁移往返 + 面试同意/隐私组合用例已在 PostgreSQL 上 16/16 通过，修复后全量 PostgreSQL 回归通过。

## 3. R3 阶段状态

| 阶段 | 状态 | 验收摘要 |
|---|---|---|
| C0 可信度与维护基线 | 通过 | 私有 JD 隔离；移除名校/名企信号和伪概率；LLM 输入最小化 |
| C1 有目标岗位主链路 | 通过 | 私有 JD 可恢复；未写能力默认为需确认；发展结论需明确确认 |
| C2 同领域三方向 | 通过 | 当前/相邻/挑战方向使用职业族和偏好政策；无目标浏览器链路通过 |
| C3 机会准备卡与面试 | 通过 | 五栏限制、证据边界、一键带入针对性面试、Observation 确认门禁 |
| C4 结果回写与复盘 | 待下一轮 | 下一阶段应统一站内/站外结果时间线与可追溯建议变化 |

## 4. 自动化验收证据

| 门禁 | 结果 |
|---|---|
| 后端 SQLite 全量回归 | 通过 |
| 后端 PostgreSQL 全量回归 | 通过 |
| PostgreSQL 并发专项 | 7/7 通过 |
| Ruff 确定性错误检查 | 通过 |
| Ruff 格式检查 | 153/153 文件通过 |
| Mypy CI 范围 | 通过 |
| 前端单元/文案门禁 | 53/53 通过 |
| ESLint | 通过 |
| Vite 生产构建 | 通过 |
| Playwright：有目标岗位 | 通过 |
| Playwright：无目标岗位三方向 | 通过 |
| Git diff 空白/冲突检查 | 通过 |

## 5. 下一阶段建议

C4 不应从“自动推断拒绝原因”开始。应先实现可追溯的结果事件：`platform_observed`、`employer_confirmed`、`candidate_reported`，再让结果只调整下一轮追问和优先级，不自动改写能力事实。
