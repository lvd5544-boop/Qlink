# QLink R3 执行与验收报告

> 日期：2026-08-09<br>
> 范围：R3 C0–C4，当前重点为 C4“结果回写与复盘”首发范围。

> 2026-08-13 增量：C4“结果回写与复盘”首发范围已经通过；C4-E v1.3 已产出 33 个分级相关性对、可复现基线、对照指标、公平性反事实、四类压力测试和双人盲标/分歧工具。C5 首个切片新增有界假设与 synthetic/real-confirmed 隔离评测。独立真人复核仍为 `pending 0/2`，C5 真实用户效果仍待试点。

## 1. 结论

R3 已推进到 C4 首发范围。当前代码、前端构建、规则与类型门禁、SQLite 全量回归、PostgreSQL 并发门禁，以及包含 C4 结果时间线在内的真实浏览器主链路均已通过。

本报告的“通过”表示所列风险和场景已被自动化验证，不表示软件在所有未知环境中绝对不可能存在缺陷。

## 2. 本轮完成内容

### GitHub 对外交付

- 保留可运行 Demo 与生产式 Docker Compose 交付；
- 根目录 README 重写为中英双语项目首页；
- 前端默认 Vite README 替换为 QLink 项目说明；
- 将 Cursor、历史 PR 报告、本地工作目录和演示文稿工作区纳入上传过滤；
- 39 份已跟踪历史过程文档已从 Git 索引移除，本地文件保留；
- 对外保留 README、R3 总计划、本执行报告、贡献规范、公平性基线和人工验收指南。
- 新增双语五分钟项目案例页，将系统架构、合成端到端案例、排序实验、失败修复、AI 辅助声明和申请表达连接到可复现证据；不宣称真人盲标或真实招聘效果已经完成。

### 结构与命名

- `CandidateApplicationKit` → `OpportunityPreparationCard`；
- `CareerDirectionExplorer` → `CareerPathExplorer`；
- `candidateRouteState` → `requirementReadinessState`；
- 机会准备的纯数据投影与界面组件分离，可单独测试；
- 159 个 Python 文件已经过统一 Ruff 格式化检查。

### Phase C3：机会准备卡与面试

- 申请素材改为五个短栏目：现在可用、面试故事、待说清、一个行动、未解决要求；
- 每条短内容最长 90 字，数量上限按 R3 规则固定；
- 证据冲突和未确认 Claim 不会进入“现在可用”；
- 未确认经历只进入待说清问题；
- TXT 导出降级为次要操作；
- 机会准备卡可将同一份简历和目标岗位直接带入结构化面试；
- 面试观察结果仍需用户确认后才能写回；
- 新增面试入口对他人私有 JD 的 404 所有权门禁。

### 验收中发现并修复

- PostgreSQL 全量回归首次执行发现，正式 PR12 迁移已包含 `interview_results.structured_session_id`，但测试库的增量初始化只补到 PR5.1；
- 测试库初始化现已补入正式 `20260729_pr12_interview_sessions_up.sql`，历史 PR5.1 迁移往返测试结束前也会恢复当前 PR12 schema；
- 历史迁移往返 + 面试同意/隐私组合用例已在 PostgreSQL 上 16/16 通过，修复后全量 PostgreSQL 回归通过。

## 3. R3 阶段状态

| 阶段 | 状态 | 验收摘要 |
|---|---|---|
| C0 可信度与维护基线 | 通过 | 私有 JD 隔离；移除名校/名企信号和伪概率；LLM 输入最小化 |
| C1 有目标岗位主链路 | 通过 | 私有 JD 可恢复；未写能力默认为需确认；发展结论需明确确认 |
| C2 同领域三方向 | 通过 | 当前/相邻/挑战方向使用职业族和偏好政策；无目标浏览器链路通过 |
| C3 机会准备卡与面试 | 通过 | 五栏限制、证据边界、一键带入针对性面试、Observation 确认门禁 |
| C4 结果回写与复盘 | 通过（首发范围） | 统一时间线、三类来源、发生/记录时间、站外原始反馈、建议变化快照和事实不变边界均通过 |
| C4-E 排序评测 | v1.3 工程门禁通过，独立复核待完成 | 33 对分级标签、TF-IDF/结构化对照、鲁棒性、反事实、盲标模板、加权 kappa 和分歧记录均可复现 |
| C5 推断假设与隔离评测 | 首个工程切片通过 | 每项最多两条；来源/规则/状态持久化；本人确认不创建 Claim；合成与真实确认池隔离 |

### C4 第一切片：结果时间线

- 将现有 `status_history` 投影为统一只读时间线，无需为了首个读模型提前新增完整事件表；
- 来源统一为 `platform_observed`、`employer_confirmed`、`candidate_reported`；
- API 不向时间线暴露内部 actor ID；
- 站外结果允许用户记录真实发生时间和最多 2,000 字的企业原始反馈；
- 拒绝且无具体反馈不会自动生成能力缺口；
- 候选人页面同时展示来源、发生时间、记录时间和原始反馈；
- 站外结果仍是候选人自报，界面不会伪装成平台或招聘方已验证。
- 每次新状态事件保存当时的“之前重点、现在重点、变化原因和不变边界”；
- 进入面试只把重点转向真实面试故事，录用不外推，拒绝无反馈只进入流程复盘；
- 旧状态记录继续通过兼容投影生成解释，新规则不会覆盖新事件已经保存的历史快照。

## 4. 自动化验收证据

| 门禁 | 结果 |
|---|---|
| 后端 SQLite 全量回归 | 通过 |
| 后端 PostgreSQL 全量回归 | 通过 |
| PostgreSQL 并发专项 | 7/7 通过 |
| Ruff 确定性错误检查 | 通过 |
| Ruff 格式检查 | 159/159 文件通过 |
| Mypy CI 范围 | 通过 |
| 前端单元/文案门禁 | 56/56 通过 |
| ESLint | 通过 |
| Vite 生产构建 | 通过 |
| Playwright：有目标岗位 | 1/1 通过（C4-E 政策修复后复验） |
| Playwright：无目标岗位三方向 | 1/1 通过（C4-E 政策修复后复验） |
| Playwright：C4 站外结果与统一时间线 | 1/1 通过（隔离 PostgreSQL + Redis） |
| C4-E 排序评测与标注专项 | 21/21 通过（含四类压力用例、盲标模板、加权 kappa 和分歧记录） |
| C5 假设专项 | 19/19 后端聚焦用例通过；3/3 合成评测通过；真实确认样本使用数 0 |
| Git diff 空白/冲突检查 | 通过 |

## 5. 下一阶段建议

下一门禁不是继续写模型，而是邀请两名真实独立复核者使用不同随机顺序完成 33 对盲标，达到协议中的一致性门禁并裁决所有分歧。完成前不引入 embeddings 或自训练模型，也不声称已经外部验证。
