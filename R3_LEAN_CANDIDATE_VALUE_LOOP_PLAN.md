# QLink R3-Lite：求职者价值闭环最小实施方案

> 日期：2026-08-03  
> 决策：优先形成有实际价值的连续产品，不建设平行系统，不用功能数量代表产品成功。  
> 首发：中国大陆；互联网/AI 岗位；应届到工作三年内。  
> 与 `CURSOR_THIRD_ROUND_IMPLEMENTATION_SPEC.md` 的关系：本方案是先行的轻量验证切片。商业信号成立后，再决定是否需要完整 `OpportunityWorkflow / RequirementRoute / ApplicationPackageDraft` 数据模型。

## 1. 产品只解决一个连续问题

求职者真正要完成的不是“使用简历、面试、证据库等多个功能”，而是：

```text
了解自己已经做过什么
  → 看懂外部真实岗位需要什么
  → 分清表达、证据、能力与现实条件差距
  → 知道现在怎么投递
  → 知道未来 1–2 周如何提升
  → 用投递结果修正下一次行动
```

首发承诺：

> 粘贴一份你真正想申请的岗位，QLink 根据你的真实经历告诉你：现在可以怎么投、还有什么需要说清，以及最值得完成的一个竞争力提升行动。

## 2. 不新增系统，复用已有能力

| 用户任务 | 当前已有能力 | 本轮处理 |
|---|---|---|
| 了解自己 | Career Passport、ResumeClaim、Evidence Vault | 工作台显示摘要和来源链接，不复制编辑系统 |
| 导入外部岗位 | `POST /advisor/target-jobs/import` | 在 Advisor 增加 JD 粘贴入口 |
| 看懂岗位 | TargetRoleProfile、JobRequirement | 首屏展示 5–8 项岗位要求 |
| 判断差距 | `OptimizationIssue`、现有诊断分类 | 重排为“现在能用/需要说清/需要提升/现实条件” |
| 个性化行动 | `build_job_guidance`、`PersonalizedGuidancePanel`、`ReadinessAction` | 每次只突出 1 个最高优先级行动 |
| 忠实改写 | `ResumePatchProposal`、Fidelity 门禁、ResumeVersion | 提供“生成当前可投版本”入口 |
| 投递与结果 | Applications、AppliedJobs、状态机 | 引导记录投递结果，不另建申请系统 |
| 商业验证 | 现有套餐和计量骨架 | 先人工收费与复访，不接正式支付渠道 |

第一阶段明确不新增：

- `OpportunityWorkflow`；
- `RequirementRoute` 和事件表；
- `ApplicationPackageDraft`；
- 新的职业市场数据库；
- 新的聊天系统、任务系统或投递状态机；
- PDF/DOCX 申请包生成；
- Explore 三方向的完整产品化。

这些对象只有在真实用户出现跨 JD 复用、历史状态追踪或团队审计需求后再建设。

## 3. 一个页面完成主流程

保留 `/candidate/advisor`，将其从“岗位画像 + 顾问聊天”调整为一个连续工作台。

### 第一步：我是谁

用户选择已有简历。页面只展示：

- 最近两到四段核心经历；
- 已确认的 Claim/证据数量；
- 缺少基础信息时跳转 Career Passport；
- 不展示大号体检分、完整度或匹配总分。

### 第二步：我想申请什么

提供两个入口，按优先级排列：

1. **粘贴真实 JD（主入口）**；
2. 从平台已有岗位选择。

粘贴入口直接复用：

```http
POST /advisor/target-jobs/import
{
  "title": "AI 产品经理",
  "description_text": "用户粘贴的完整 JD"
}
```

接口已经返回私有岗位 ID、岗位画像和要求。前端收到后直接把返回的私有岗位加入当前页面选择项并设为选中，不依赖 `/browse-jobs` 返回私有岗位。

### 第三步：外部岗位需要什么

展示 5–8 项要求，并分成：

- 必须条件；
- 重点能力；
- 背景参考。

第一版使用已有解析和来源信息。候选人导入的 JD 标注“来自你粘贴的岗位原文”，不得暗示已获企业确认或代表整个市场趋势。

“了解外部市场”在首发阶段定义为理解具体真实 JD 和同岗位族的共同能力语言，不承诺实时薪资预测、企业录用偏好或市场前景排名。

### 第四步：我与岗位之间是什么关系

调用已有诊断：

```http
POST /advisor/jobs/{job_id}/diagnostics
{
  "resume_id": "..."
}
```

不先展示 readiness 分数。使用现有 `issues/categories/claim_ids/strategies` 重排成四个用户可行动区：

| 页面区 | 判断条件 | 用户动作 |
|---|---|---|
| 现在可以使用 | 有可追溯 Claim，策略 `can_apply_now=true` | 生成忠实改写或面试故事素材 |
| 需要说清楚 | 有相关 Claim，但证据、角色、方法或结果不完整 | 回答一个问题或补充 Claim |
| 建议提升 | 当前只有未来行动策略；不得直接写成“你不会” | 用户确认后选择一个 1–2 周行动 |
| 现实条件 | `hard_constraint` | 单独确认地点、学历、证书等 |

第一版不要求把四区持久化成新的状态机。继续以 `OptimizationIssue` 为后端事实来源，并保留“系统建议，等待用户确认”的文案。

### 第五步：现在怎么投

页面给出一个主按钮：**生成当前可投版本**。

第一版不生成新的综合申请包对象，而是：

- 汇总当前可应用的 `OptimizationIssue`；
- 复用 `ResumePatchProposal` 逐项预览和事实确认；
- 生成新的不可变 ResumeVersion；
- 清楚标注哪些要求尚未解决；
- 提供“去投递/记录投递”入口。

不把未确认内容、JD 文本或未来行动写入简历。

### 第六步：下一步如何提升

不要展示一长串课程或建议。只突出一项：

```text
为什么对这个岗位重要
→ 从用户哪段经历开始
→ 今天的第一步
→ 1–2 周内交付什么
→ 完成后如何成为新的 Claim/Evidence
```

复用 `PersonalizedGuidancePanel` 和 `ReadinessAction`。任务勾选完成不能自动提高准备度，必须补充新事实或证据后重新诊断。

### 第七步：结果写回

复用 AppliedJobs/Application 状态。用户投递后只询问：

- 已投递；
- 进入面试；
- 未通过；
- 获得录用；
- 暂无结果。

首轮不做因果归因，只把结果用于下次提醒用户重新分析、补充证据或调整目标。

## 4. 最小代码改动

### VL-01：打通真实 JD 入口

主要修改：

- `frontend/src/pages/Candidate/Advisor.jsx`；
- 可新建一个小组件 `TargetJobImportModal.jsx`；
- 增加前端 copy/contract test。

不改数据库，不新增后端接口。复用 `/advisor/target-jobs/import`，并在前端直接接收私有岗位，不要求 `/browse-jobs` 暴露它。

验收：用户粘贴 JD 后无需离开页面即可选择简历并生成诊断；刷新后可通过现有 job ID 重新读取私有岗位画像。

### VL-02：把诊断改成行动地图

主要修改：

- `Advisor.jsx`；
- 新建 `CandidateActionMap.jsx`，只做现有响应的展示映射；
- 复用 `PersonalizedGuidancePanel.jsx`；
- 隐藏第一屏 readiness 大号分数，将其降级到折叠参考区。

不新增路由表或状态机。四区标签必须通过现有来源和策略字段确定；无法可靠判断的项目显示“需要确认”，不得显示“能力不足”。

验收：用户能在一个屏幕回答“岗位要什么、我现在有什么、今天做什么”。

### VL-03：连接当前可投版本与行动

主要修改：

- 从 Advisor 跳转或嵌入现有 `TargetJobOptimizationPanel`；
- 传递当前 `resume_id/job_id`；
- 将已有忠实改写和 `ReadinessAction` 放到同一条下一步路径；
- 增加“记录投递”入口到现有 AppliedJobs。

不新增 Application Package 数据模型。

验收：用户可以从真实 JD 开始，完成至少一项忠实改写、得到一个提升行动，并进入投递记录。

### VL-04：人工商业试点

不等待完整 R3-06 工具。本包与 VL-01～VL-03 并行准备，在 VL-02 可用后开始陪跑。

- 20 名中国互联网/AI 真实求职者；
- 每人带一份真实 JD 和至少两段经历；
- 一半先使用通用大模型，一半先使用 QLink，再交叉体验；
- 人工记录首次有用产出时间、行动改变、7 日第二行为、实际采用内容；
- 免费完成第一次分析；测试 ¥39/¥59 解锁第二个 JD 或完整忠实改写流程；
- 人工收款或可退押金，不开发正式支付系统。

## 5. 商业成功门禁

只有以下条件成立才继续完整 R3 数据模型：

- ≥ 70% 用户能说出一个具体行动改变；
- ≥ 50% 用户在 7 天内完成第二次有效行为；
- ≥ 5 人真实支付 ¥39–59 或同额可退押金；
- 相比通用大模型，“下一步更有用”提升 ≥ 15 个百分点；
- ≥ 30% 用户把至少一项改写或故事素材用于真实申请；
- 事实编造率为 0。

决策：

- **Go**：上述门禁通过，再建设跨 JD 历史、正式五状态事件和综合申请包；
- **Iterate**：用户有行动改变但不复用/不付款，收窄岗位族或调整收费时点；
- **Stop/Pivot**：相对通用大模型没有明显提升，停止扩建工作流数据模型。

## 6. 防止再次堆功能的规则

新增任何功能前必须回答：

1. 它推动“了解自己、看懂岗位、投递、提升、结果写回”中的哪一步？
2. 没有它，真实用户是否无法完成当前任务？
3. 现有对象、接口或页面是否已经能完成 70%？
4. 它会改善哪个商业门禁？

如果四个问题不能明确回答，本轮不做。

当前优先级：

```text
真实 JD 入口
  > 一屏行动地图
  > 当前可投版本
  > 一个提升行动
  > 结果记录与七日复访
  > Explore 方向
  > 新数据模型
  > 更多页面与功能
```

最终产品不是一个功能集合，而是一个反复运行的个人求职决策循环：

> 我有什么 → 市场要什么 → 差在哪里 → 现在怎么投 → 接下来怎么变强 → 结果告诉我下一步做什么。
